import React from "react";

const Partners = () => {
  return (
    <div className="overflow-hidden relative w-full h-[40px] md:h-[50px]  border-t border-t-[var(--softBg4)] bg-t-[var(--bg)]
     whitespace-nowrap p-3 md:p-9 fixed  bottom-0 ">
      <div className="flex  gap-9  md:gap-[80px] animate-scroll">
        <h1 className="text-xl md:text-3xl">Everyone is welcome to the house of the lord</h1>
        
        <h1 className="text-xl md:text-3xl">Everyone is welcome to the house of the lord</h1>

      </div>
    </div>
  );
};

export default Partners;

// Tailwind CSS Styles (Add to your global CSS or tailwind config)
// Add this to your CSS file or global styles
// If using Tailwind, include this in your styles
